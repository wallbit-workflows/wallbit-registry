import { clerkAppearance } from "@/lib/clerk-appearance";

/** Shared Clerk modal appearance (petdex-style: modal only, no custom OAuth routes). */
export const clerkModalAppearance = clerkAppearance;
